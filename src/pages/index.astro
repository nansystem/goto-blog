---
import { getListing } from '../lib/cms'
import Listing from '../components/Listing.astro'
const data = await getListing(1, undefined)
if (!data) return Astro.rewrite('/404')
---

<Listing data={data} />
